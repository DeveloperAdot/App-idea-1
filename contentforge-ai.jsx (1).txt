import { useState, useEffect, useRef, useCallback } from "react";
import {
  Sparkles, ArrowRight, Check, Zap, Copy, RefreshCw,
  FileText, Mail, Youtube, Search, TrendingUp, Users,
  Clock, Star, LogOut, Play, Shield, Hash, Layers,
  Target, CheckCircle2, AlertCircle, Loader2,
  Megaphone, LayoutDashboard, X, ChevronRight,
  ChevronLeft, Pause, Eye, Flame, Rocket
} from "lucide-react";

/* ══════════════════════════════════════════════════════════════════
   DESIGN SYSTEM — Vibrant Multicolour
══════════════════════════════════════════════════════════════════ */
const CSS = `
  @import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:ital,wght@0,300;0,400;0,500;0,600;0,700;0,800;1,400&family=Syne:wght@400;500;600;700;800&family=DM+Mono:wght@400;500&display=swap');

  :root {
    --bg: #080812;
    --bg2: #0D0D1F;
    --surface: rgba(255,255,255,0.04);
    --surface2: rgba(255,255,255,0.07);
    --border: rgba(255,255,255,0.09);
    --border-bright: rgba(255,255,255,0.18);
    --text: #F2F0FF;
    --text2: #9896B8;
    --text3: #5E5C80;

    /* Vivid palette */
    --violet:  #7C3AED;
    --violet2: #A78BFA;
    --pink:    #EC4899;
    --pink2:   #F472B6;
    --cyan:    #06B6D4;
    --cyan2:   #67E8F9;
    --lime:    #84CC16;
    --lime2:   #BEF264;
    --amber:   #F59E0B;
    --amber2:  #FCD34D;
    --rose:    #F43F5E;
    --rose2:   #FB7185;
    --indigo:  #6366F1;
    --indigo2: #818CF8;
    --teal:    #14B8A6;
    --teal2:   #5EEAD4;
    --orange:  #F97316;
    --orange2: #FB923C;

    --radius: 14px;
    --radius-lg: 22px;
    --radius-xl: 32px;
  }

  * { box-sizing: border-box; margin: 0; padding: 0; }

  body {
    background: var(--bg);
    color: var(--text);
    font-family: 'Plus Jakarta Sans', sans-serif;
    font-size: 15px;
    line-height: 1.6;
    overflow-x: hidden;
  }

  ::-webkit-scrollbar { width: 4px; }
  ::-webkit-scrollbar-track { background: var(--bg); }
  ::-webkit-scrollbar-thumb {
    background: linear-gradient(180deg, var(--violet), var(--pink));
    border-radius: 2px;
  }

  .mono { font-family: 'DM Mono', monospace; }
  .syne { font-family: 'Syne', sans-serif; }

  /* ── Gradient text utilities ── */
  .g-violet { background: linear-gradient(135deg, var(--violet2), var(--pink2)); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; }
  .g-cyan   { background: linear-gradient(135deg, var(--cyan), var(--teal2));   -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; }
  .g-amber  { background: linear-gradient(135deg, var(--amber), var(--amber2));  -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; }
  .g-rainbow { background: linear-gradient(90deg, #A78BFA, #F472B6, #FB923C, #FCD34D); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; }
  .g-hero { background: linear-gradient(135deg, #F2F0FF 0%, #C4B5FD 40%, #F9A8D4 70%, #FDE68A 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text; }

  /* ── Form elements ── */
  textarea, input, select {
    background: rgba(255,255,255,0.05);
    border: 1px solid var(--border-bright);
    border-radius: var(--radius);
    color: var(--text);
    font-family: 'Plus Jakarta Sans', sans-serif;
    font-size: 14px;
    outline: none;
    width: 100%;
    padding: 13px 16px;
    transition: border-color .2s, box-shadow .2s, background .2s;
  }
  textarea:focus, input:focus {
    border-color: rgba(124,58,237,0.6);
    background: rgba(124,58,237,0.06);
    box-shadow: 0 0 0 3px rgba(124,58,237,0.12);
  }
  textarea { resize: vertical; min-height: 120px; }
  select { padding: 11px 16px; cursor: pointer; }

  /* ── Animations ── */
  @keyframes fadeUp    { from{opacity:0;transform:translateY(24px)} to{opacity:1;transform:translateY(0)} }
  @keyframes fadeIn    { from{opacity:0} to{opacity:1} }
  @keyframes scaleIn   { from{opacity:0;transform:scale(0.93)} to{opacity:1;transform:scale(1)} }
  @keyframes slideIn   { from{opacity:0;transform:translateX(28px)} to{opacity:1;transform:translateX(0)} }
  @keyframes spin      { from{transform:rotate(0deg)} to{transform:rotate(360deg)} }
  @keyframes blink     { 0%,100%{opacity:1} 50%{opacity:0} }
  @keyframes shimmer   { 0%{background-position:-400px 0} 100%{background-position:400px 0} }
  @keyframes float1    { 0%,100%{transform:translate(0,0) scale(1)}   50%{transform:translate(30px,-40px) scale(1.08)} }
  @keyframes float2    { 0%,100%{transform:translate(0,0) scale(1)}   50%{transform:translate(-25px,35px) scale(1.05)} }
  @keyframes float3    { 0%,100%{transform:translate(0,0) scale(1)}   50%{transform:translate(20px,25px)  scale(1.06)} }
  @keyframes orbitPing { 0%{transform:scale(1);opacity:.6} 100%{transform:scale(2.2);opacity:0} }
  @keyframes gradShift { 0%{background-position:0% 50%} 50%{background-position:100% 50%} 100%{background-position:0% 50%} }
  @keyframes ticker    { from{transform:translateX(0)} to{transform:translateX(-50%)} }

  .fu1 { animation: fadeUp .65s .08s ease both; }
  .fu2 { animation: fadeUp .65s .18s ease both; }
  .fu3 { animation: fadeUp .65s .28s ease both; }
  .fu4 { animation: fadeUp .65s .38s ease both; }
  .fu5 { animation: fadeUp .65s .48s ease both; }
  .fade-in  { animation: fadeIn  .4s ease both; }
  .scale-in { animation: scaleIn .35s ease both; }
  .slide-in { animation: slideIn .35s ease both; }
  .spin-anim { animation: spin 1.1s linear infinite; }

  /* ── Glass card ── */
  .glass {
    background: rgba(255,255,255,0.04);
    backdrop-filter: blur(24px);
    border: 1px solid var(--border-bright);
    border-radius: var(--radius-lg);
  }
  .glass-hover {
    transition: all .25s ease;
  }
  .glass-hover:hover {
    background: rgba(255,255,255,0.07);
    border-color: rgba(255,255,255,0.22);
    transform: translateY(-3px);
    box-shadow: 0 16px 48px rgba(0,0,0,0.35);
  }

  /* ── Sidebar ── */
  .sidebar {
    width: 248px;
    min-height: 100vh;
    background: linear-gradient(180deg, rgba(13,13,31,0.98) 0%, rgba(8,8,18,0.98) 100%);
    border-right: 1px solid var(--border);
    display: flex;
    flex-direction: column;
    position: fixed;
    top: 0; left: 0;
    z-index: 40;
    backdrop-filter: blur(40px);
  }
  .main-c { margin-left: 248px; min-height: 100vh; }

  /* ── Nav item ── */
  .nav-item {
    display: flex; align-items: center; gap: 10px;
    width: 100%; padding: 10px 13px;
    border-radius: 11px; border: none;
    background: transparent;
    color: var(--text2);
    font-family: 'Plus Jakarta Sans', sans-serif;
    font-size: 13px; font-weight: 500;
    cursor: pointer; margin-bottom: 3px;
    text-align: left; transition: all .18s;
    position: relative;
  }
  .nav-item:hover { background: rgba(255,255,255,0.06); color: var(--text); }
  .nav-item.active {
    background: linear-gradient(135deg, rgba(124,58,237,0.2), rgba(236,72,153,0.12));
    color: var(--text);
    border: 1px solid rgba(124,58,237,0.3);
    font-weight: 600;
  }

  /* ── Tool button (quick-start grid) ── */
  .tool-btn {
    background: rgba(255,255,255,0.03);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 14px 16px;
    cursor: pointer; transition: all .22s;
    text-align: left; width: 100%; color: var(--text);
  }
  .tool-btn:hover {
    background: rgba(255,255,255,0.07);
    border-color: var(--border-bright);
    transform: translateY(-2px);
    box-shadow: 0 8px 32px rgba(0,0,0,0.3);
  }

  /* ── Output block ── */
  .output-block {
    background: rgba(255,255,255,0.03);
    border: 1px solid var(--border-bright);
    border-radius: var(--radius);
    padding: 18px;
    font-size: 13.5px;
    line-height: 1.75;
    white-space: pre-wrap;
    max-height: 520px;
    overflow-y: auto;
  }

  /* ── Stat card ── */
  .stat-card {
    border-radius: var(--radius);
    padding: 20px;
    transition: all .22s;
    position: relative;
    overflow: hidden;
  }
  .stat-card:hover { transform: translateY(-3px); }

  /* ── Progress ── */
  .prog-track { height: 5px; background: rgba(255,255,255,0.07); border-radius: 3px; overflow: hidden; }
  .prog-fill  { height: 100%; border-radius: 3px; transition: width .9s ease; }

  /* ── Pricing card ── */
  .pcard {
    border-radius: var(--radius-lg);
    padding: 32px;
    transition: all .3s;
    position: relative;
    overflow: visible;
  }
  .pcard:hover { transform: translateY(-5px); }

  /* ── Demo modal ── */
  .demo-overlay {
    position: fixed; inset: 0;
    background: rgba(0,0,0,0.88);
    backdrop-filter: blur(16px);
    z-index: 1000;
    display: flex; align-items: center; justify-content: center;
    padding: 16px;
    animation: fadeIn .3s ease;
  }
  .demo-modal {
    background: linear-gradient(145deg, #0F0F24, #151530);
    border: 1px solid rgba(255,255,255,0.12);
    border-radius: 26px;
    width: 100%; max-width: 940px; max-height: 92vh;
    overflow: hidden;
    display: flex; flex-direction: column;
    box-shadow: 0 48px 140px rgba(0,0,0,0.8), 0 0 0 1px rgba(124,58,237,0.2);
    animation: scaleIn .35s ease;
  }
  .demo-screen {
    background: rgba(0,0,0,0.4);
    border: 1px solid var(--border-bright);
    border-radius: 16px;
    padding: 18px;
    min-height: 300px;
    position: relative;
    overflow: hidden;
  }
  .shimmer-line {
    height: 11px; border-radius: 4px;
    background: linear-gradient(90deg, rgba(255,255,255,0.04) 0%, rgba(124,58,237,0.2) 50%, rgba(255,255,255,0.04) 100%);
    background-size: 400px 100%;
    animation: shimmer 1.5s ease infinite;
  }
  .cursor {
    display: inline-block; width: 2px; height: 1em;
    background: var(--violet2); margin-left: 1px;
    animation: blink .8s ease infinite; vertical-align: middle;
  }
  .demo-nav-btn {
    display: flex; align-items: center; gap: 7px;
    padding: 10px 20px; border-radius: 11px; border: none;
    cursor: pointer; font-family: 'Plus Jakarta Sans', sans-serif;
    font-size: 13px; font-weight: 600; transition: all .2s;
  }

  /* ── Ticker tape ── */
  .ticker-wrap { overflow: hidden; border-top: 1px solid var(--border); border-bottom: 1px solid var(--border); padding: 12px 0; background: rgba(255,255,255,0.02); }
  .ticker-inner { display: flex; gap: 56px; animation: ticker 28s linear infinite; white-space: nowrap; width: max-content; }

  /* ── Glow orbs ── */
  .orb { position: absolute; border-radius: 50%; filter: blur(80px); pointer-events: none; }

  /* ── Animated gradient button ── */
  .grad-btn {
    background: linear-gradient(135deg, var(--violet), var(--pink), var(--orange));
    background-size: 200% 200%;
    animation: gradShift 4s ease infinite;
    border: none; cursor: pointer; font-family: 'Plus Jakarta Sans', sans-serif;
    font-weight: 700; transition: opacity .2s, transform .2s, box-shadow .2s;
  }
  .grad-btn:hover { opacity: .92; transform: translateY(-1px); box-shadow: 0 8px 32px rgba(124,58,237,0.4); }

  /* ── Feature card ── */
  .feat-card {
    border-radius: var(--radius-lg); padding: 24px;
    transition: all .25s; cursor: default;
    position: relative; overflow: hidden;
  }
  .feat-card::before {
    content: ''; position: absolute; inset: 0; border-radius: var(--radius-lg);
    background: linear-gradient(135deg, rgba(255,255,255,0.04), transparent);
    opacity: 0; transition: opacity .25s;
  }
  .feat-card:hover::before { opacity: 1; }
  .feat-card:hover { transform: translateY(-4px); box-shadow: 0 20px 60px rgba(0,0,0,0.4); }

  @media (max-width: 768px) {
    .sidebar { display: none; }
    .main-c { margin-left: 0; }
    .demo-modal { max-height: 96vh; border-radius: 18px; }
  }
`;

/* ══════════════════════════════════════════════════════════════════
   DATA
══════════════════════════════════════════════════════════════════ */
const TOOLS = [
  { id:"repurpose", icon:RefreshCw,  label:"Content Repurposer",    color:"#7C3AED", color2:"#A78BFA", grad:"linear-gradient(135deg,#7C3AED,#EC4899)", desc:"Turn 1 blog into 10+ formats" },
  { id:"blog",      icon:FileText,   label:"Blog Post Writer",       color:"#6366F1", color2:"#818CF8", grad:"linear-gradient(135deg,#6366F1,#06B6D4)", desc:"SEO-optimised long-form content" },
  { id:"social",    icon:Hash,       label:"Social Media Pack",      color:"#10B981", color2:"#5EEAD4", grad:"linear-gradient(135deg,#10B981,#84CC16)", desc:"Platform-native posts instantly" },
  { id:"email",     icon:Mail,       label:"Email Campaign",         color:"#F59E0B", color2:"#FCD34D", grad:"linear-gradient(135deg,#F59E0B,#F97316)", desc:"High-converting email sequences" },
  { id:"youtube",   icon:Youtube,    label:"YouTube Script",         color:"#EF4444", color2:"#FB7185", grad:"linear-gradient(135deg,#EF4444,#F97316)", desc:"Hooks, body & CTA scripts" },
  { id:"seo",       icon:Search,     label:"SEO Meta Generator",     color:"#06B6D4", color2:"#67E8F9", grad:"linear-gradient(135deg,#06B6D4,#6366F1)", desc:"Titles, descriptions & keywords" },
  { id:"ads",       icon:Megaphone,  label:"Ad Copy Generator",      color:"#F97316", color2:"#FB923C", grad:"linear-gradient(135deg,#F97316,#EC4899)", desc:"Google, Meta & LinkedIn ads" },
  { id:"product",   icon:Target,     label:"Product Descriptions",   color:"#EC4899", color2:"#F9A8D4", grad:"linear-gradient(135deg,#EC4899,#8B5CF6)", desc:"Persuasive e-commerce copy" },
];

const NAV = [
  { id:"dashboard", icon:LayoutDashboard, label:"Dashboard",          grad:"linear-gradient(135deg,#7C3AED,#EC4899)" },
  { id:"repurpose", icon:RefreshCw,       label:"Repurposer",         grad:"linear-gradient(135deg,#7C3AED,#EC4899)" },
  { id:"blog",      icon:FileText,        label:"Blog Writer",        grad:"linear-gradient(135deg,#6366F1,#06B6D4)" },
  { id:"social",    icon:Hash,            label:"Social Pack",        grad:"linear-gradient(135deg,#10B981,#84CC16)" },
  { id:"email",     icon:Mail,            label:"Email Campaigns",    grad:"linear-gradient(135deg,#F59E0B,#F97316)" },
  { id:"youtube",   icon:Youtube,         label:"YouTube Scripts",    grad:"linear-gradient(135deg,#EF4444,#F97316)" },
  { id:"seo",       icon:Search,          label:"SEO Meta",           grad:"linear-gradient(135deg,#06B6D4,#6366F1)" },
  { id:"ads",       icon:Megaphone,       label:"Ad Copy",            grad:"linear-gradient(135deg,#F97316,#EC4899)" },
  { id:"product",   icon:Target,          label:"Product Copy",       grad:"linear-gradient(135deg,#EC4899,#8B5CF6)" },
];

const STATS_GLOBAL = [
  { v:"2.4M+",   label:"Content Generated",  icon:FileText,   grad:"linear-gradient(135deg,#7C3AED,#A78BFA)", glow:"rgba(124,58,237,0.25)" },
  { v:"12,400",  label:"Active Businesses",   icon:Users,      grad:"linear-gradient(135deg,#10B981,#5EEAD4)", glow:"rgba(16,185,129,0.25)" },
  { v:"94,000",  label:"Hours Saved Weekly",  icon:Clock,      grad:"linear-gradient(135deg,#F59E0B,#FCD34D)", glow:"rgba(245,158,11,0.25)" },
  { v:"380%",    label:"Average ROI",         icon:TrendingUp, grad:"linear-gradient(135deg,#EC4899,#F9A8D4)", glow:"rgba(236,72,153,0.25)" },
];

const PLANS = [
  { name:"Starter", price:19, desc:"For solo creators", badge:null, feat:false, cta:"Start Free Trial",
    features:["50 AI generations/mo","6 content tools","Markdown export","Email support","1 workspace"],
    grad:"linear-gradient(135deg,rgba(99,102,241,0.12),rgba(6,182,212,0.08))", border:"rgba(99,102,241,0.3)" },
  { name:"Pro", price:49, desc:"For growing businesses", badge:"MOST POPULAR", feat:true, cta:"Get Started",
    features:["500 AI generations/mo","All 8 content tools","Priority generation","API access","3 workspaces","Analytics dashboard"],
    grad:"linear-gradient(135deg,rgba(124,58,237,0.2),rgba(236,72,153,0.12))", border:"rgba(124,58,237,0.5)" },
  { name:"Agency", price:149, desc:"Unlimited for teams", badge:null, feat:false, cta:"Talk to Sales",
    features:["Unlimited generations","All tools + early access","White-label export","Dedicated support","Unlimited workspaces","Team roles & permissions","Custom fine-tuning"],
    grad:"linear-gradient(135deg,rgba(236,72,153,0.12),rgba(249,115,22,0.08))", border:"rgba(236,72,153,0.3)" },
];

const TESTIMONIALS = [
  { name:"Sarah Chen",  role:"Content Director @ Bloom Agency", text:"We replaced 3 freelancers with ContentForge. Output tripled in week one. The repurposer alone paid for 12 months.", avatar:"SC", grad:"linear-gradient(135deg,#7C3AED,#EC4899)", stars:5 },
  { name:"Marcus Webb", role:"Founder, GrowthStack",             text:"Was spending 20 hours writing per week. Now it's 2. The quality is indistinguishable from my own voice after a few sessions.", avatar:"MW", grad:"linear-gradient(135deg,#06B6D4,#6366F1)", stars:5 },
  { name:"Priya Nair",  role:"E-commerce Brand Manager",         text:"Product descriptions, ads, emails — all in one place. Conversion rate up 34% in month one. Absolute game changer.", avatar:"PN", grad:"linear-gradient(135deg,#10B981,#84CC16)", stars:5 },
];

const SYSTEM_PROMPTS = {
  repurpose:`You are ContentForge AI, expert content repurposing engine. Given source content, create:
1. 📝 BLOG INTRO (150 words, SEO-friendly)
2. 🐦 TWITTER/X THREAD (5 numbered tweets under 280 chars each, hook tweet first)
3. 💼 LINKEDIN POST (professional, 200 words, 3 hashtags)
4. 📸 INSTAGRAM CAPTION (emojis, 150 words, 5 hashtags)
5. 📧 EMAIL SUBJECT LINES (5 A/B test options)
6. 🎬 VIDEO HOOK (30-second spoken script opening)
Use bold emoji headers. Be specific and ready-to-publish.`,
  blog:`You are ContentForge AI, expert SEO blog writer. Write a complete, high-quality blog post: engaging H1, compelling 2-sentence hook, H2/H3 subheadings, 700-800 words, actionable insights, specific examples, strong CTA. Authoritative but approachable tone.`,
  social:`You are ContentForge AI, social media specialist. Create a full pack:
🐦 TWITTER/X — 3 tweets (hook/value/CTA, under 280 chars each)
💼 LINKEDIN — 200-word professional post + 3 hashtags
📸 INSTAGRAM — emoji-rich, 150 words + 10 hashtags
📌 PINTEREST — keyword-rich, 100 words
🎵 TIKTOK/REELS — 60-second spoken script with pattern interrupts`,
  email:`You are ContentForge AI, direct response email copywriter. Write a 3-part sequence:
EMAIL 1 — WELCOME: Subject + 200-word value-delivering email
EMAIL 2 — STORY: Subject + 200-word pain-agitation email
EMAIL 3 — OFFER: Subject + 200-word solution/CTA email
Each includes a P.S. line. Feel human and conversational.`,
  youtube:`You are ContentForge AI, YouTube scriptwriter. Write a complete script:
🎣 HOOK (15 sec — stop the scroll, create curiosity)
📋 INTRO (30 sec — credibility + preview)
🎬 MAIN (3-5 segments with transitions)
📣 CTA (subscribe prompt + comment question)
Include [VISUAL CUE] notes. Write for spoken delivery.`,
  seo:`You are ContentForge AI, SEO expert. Generate:
🎯 PRIMARY KEYWORD, 📝 META TITLE (under 60 chars), 📄 META DESCRIPTION (under 155 chars)
🏷️ 10 LSI KEYWORDS, 📋 H1 TAG, 🔗 URL SLUG
📊 5 H2 subheadings for content brief, 🌟 SCHEMA TYPE`,
  ads:`You are ContentForge AI, performance ad copywriter. Create:
🔍 GOOGLE ADS — 3 headlines (max 30 chars), 2 descriptions (max 90 chars)
📘 FACEBOOK/META — primary text 125 words, headline + description
💼 LINKEDIN — 150-word intro, headline under 70 chars
Use power words and psychological triggers.`,
  product:`You are ContentForge AI, e-commerce conversion copywriter. Write:
🏷️ PRODUCT TITLE (SEO, benefit-led, under 80 chars)
✨ SHORT DESCRIPTION (2-sentence hook)
📦 FULL DESCRIPTION (300 words — benefits first, features woven in)
✅ 5 BULLET POINTS (benefit-focused, Amazon/Shopify ready)
⭐ REVIEW PROMPT`,
};

/* ══════════════════════════════════════════════════════════════════
   DEMO TOUR DATA
══════════════════════════════════════════════════════════════════ */
const DEMO_STEPS = [
  { id:"welcome",   label:"1 / 5", title:"Welcome to ContentForge AI", subtitle:"See how 12,400+ businesses automate their entire content operation.", screen:"dashboard", narration:"ContentForge AI is your all-in-one AI content engine — eight specialised tools, all powered by Claude. Let's walk through exactly how it works in under 60 seconds.", duration:5500 },
  { id:"paste",     label:"2 / 5", title:"Paste Any Content or Idea",  subtitle:"A blog post, podcast transcript, or even just a topic — ContentForge handles it all.", screen:"input",     narration:"Start with any piece of content. ContentForge reads and understands the full context, so every output feels native and on-brand from the very first generation.", duration:7000 },
  { id:"generate",  label:"3 / 5", title:"AI Generates in Seconds",    subtitle:"Watch ContentForge write for every platform simultaneously.", screen:"generating", narration:"The AI writes for Twitter, LinkedIn, email, YouTube, and more at the same time. Each piece is tailored to the format, tone, and algorithm of its destination.", duration:5000 },
  { id:"output",    label:"4 / 5", title:"Publish-Ready Output",       subtitle:"Copy, export, or push directly to your workflow.", screen:"output",     narration:"Every output is ready to publish immediately. Your Twitter thread, LinkedIn post, email subject lines, and video hook — all done. One click to copy and you're live.", duration:8000 },
  { id:"tools",     label:"5 / 5", title:"Eight Tools, One Plan",      subtitle:"Every content format your business needs, one subscription.", screen:"tools",      narration:"From ad copy to product descriptions, SEO meta to YouTube scripts — ContentForge replaces an entire content team. Start free and publish your first pack in minutes.", duration:6000 },
];

const DEMO_OUTPUT = `🐦 TWITTER/X THREAD

1/ I grew an email list to 10,000 with ZERO paid ads. Here's the exact free traffic playbook 🧵

2/ Step 1: The SEO Foundation — Write one pillar post/week targeting "best [tool] for [audience]". My top post still drives 400 signups/month, completely on autopilot.

3/ Step 2: The LinkedIn Loop — Post your biggest insight from each article. Tag 3 tools or people you mentioned. Consistently drives 80–120 qualified clicks per post.

4/ Step 3: The Pinterest Flywheel — Create 3 pin designs per article. Pinterest traffic is evergreen — pins from 18 months ago still convert today.

5/ Step 4: The Guest Post Accelerator — One guest post/month in a niche newsletter = 300–800 highly qualified new subscribers per placement.

💼 LINKEDIN POST

Most people think you need a big ad budget to build an email list. You don't. You need a system.

Here's the free traffic framework that got me to 10,000 subscribers:

→ SEO content that compounds over time
→ LinkedIn posts that drive qualified clicks
→ Pinterest pins that convert for 18+ months
→ Strategic guest posts in niche newsletters

The key insight: free traffic is slow to start and impossible to stop. Paid traffic stops the moment you stop paying.

Build the asset that compounds.

#EmailMarketing #ContentMarketing #GrowthStrategy

📧 EMAIL SUBJECT LINES — A/B Test These

1. How I hit 10k subscribers without spending a dollar on ads
2. The free traffic playbook nobody talks about
3. Your email list isn't growing because of this one mistake
4. 4 free channels → 10,000 subscribers (full breakdown)
5. Stop paying for traffic. Do this instead.`;

/* ══════════════════════════════════════════════════════════════════
   API
══════════════════════════════════════════════════════════════════ */
async function callClaude(toolId, userMsg) {
  const res = await fetch("https://api.anthropic.com/v1/messages", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({
      model: "claude-sonnet-4-20250514",
      max_tokens: 1000,
      system: SYSTEM_PROMPTS[toolId] || SYSTEM_PROMPTS.repurpose,
      messages: [{ role: "user", content: userMsg }],
    }),
  });
  if (!res.ok) throw new Error(`HTTP ${res.status}`);
  const d = await res.json();
  return d.content.map(b => b.text || "").join("\n");
}

/* ══════════════════════════════════════════════════════════════════
   TYPEWRITER HOOK
══════════════════════════════════════════════════════════════════ */
function useTypewriter(text, active, speed = 20) {
  const [displayed, setDisplayed] = useState("");
  const [done, setDone] = useState(false);
  useEffect(() => {
    if (!active) { setDisplayed(""); setDone(false); return; }
    setDisplayed(""); setDone(false);
    let i = 0;
    const id = setInterval(() => {
      i++;
      setDisplayed(text.slice(0, i));
      if (i >= text.length) { clearInterval(id); setDone(true); }
    }, speed);
    return () => clearInterval(id);
  }, [text, active]);
  return { displayed, done };
}

/* ══════════════════════════════════════════════════════════════════
   COPY BUTTON
══════════════════════════════════════════════════════════════════ */
function CopyBtn({ text, small }) {
  const [ok, setOk] = useState(false);
  return (
    <button onClick={() => navigator.clipboard.writeText(text).then(() => { setOk(true); setTimeout(() => setOk(false), 2000); })}
      style={{ display:"flex", alignItems:"center", gap:6,
        background: ok ? "rgba(16,185,129,0.15)" : "rgba(124,58,237,0.15)",
        border: `1px solid ${ok ? "rgba(16,185,129,0.4)" : "rgba(124,58,237,0.4)"}`,
        borderRadius:9, padding: small ? "4px 10px" : "7px 14px",
        color: ok ? "#5EEAD4" : "#A78BFA", fontSize:12, fontWeight:700,
        cursor:"pointer", transition:"all .2s",
        fontFamily:"'Plus Jakarta Sans',sans-serif",
      }}>
      {ok ? <CheckCircle2 size={13}/> : <Copy size={13}/>}
      {ok ? "Copied!" : "Copy"}
    </button>
  );
}

/* ══════════════════════════════════════════════════════════════════
   DEMO SCREEN CONTENTS
══════════════════════════════════════════════════════════════════ */
function DemoScreen({ stepId }) {
  const INPUT_TEXT = "How to grow your email list from 0 to 10,000 subscribers using free traffic — no paid ads.";
  const { displayed: typed, done: typeDone } = useTypewriter(INPUT_TEXT, stepId === "paste");
  const [gen, setGen] = useState(0);
  const [outLines, setOutLines] = useState(0);

  useEffect(() => {
    if (stepId !== "generating") { setGen(0); return; }
    const id = setInterval(() => setGen(p => { if (p >= 100) { clearInterval(id); return 100; } return p + 2.2; }), 80);
    return () => clearInterval(id);
  }, [stepId]);

  useEffect(() => {
    if (stepId !== "output") { setOutLines(0); return; }
    let i = 0;
    const lines = DEMO_OUTPUT.split('\n').length;
    const id = setInterval(() => { i += 2; setOutLines(i); if (i >= lines) clearInterval(id); }, 55);
    return () => clearInterval(id);
  }, [stepId]);

  const S = { fontSize:12, borderRadius:9, padding:"9px 11px", marginBottom:8, display:"flex", alignItems:"center", gap:8 };

  if (stepId === "dashboard") return (
    <div className="scale-in">
      <div style={{ display:"grid", gridTemplateColumns:"repeat(4,1fr)", gap:8, marginBottom:12 }}>
        {[["219","Gens Used","linear-gradient(135deg,#7C3AED,#A78BFA)"],["84K","Words","linear-gradient(135deg,#06B6D4,#6366F1)"],["31h","Saved","linear-gradient(135deg,#10B981,#84CC16)"],["380%","ROI","linear-gradient(135deg,#F59E0B,#F97316)"]].map(([v,l,g])=>(
          <div key={l} style={{ background:g, borderRadius:10, padding:"11px 10px" }}>
            <p style={{ fontSize:9, color:"rgba(255,255,255,0.7)", textTransform:"uppercase", letterSpacing:"0.07em", marginBottom:3 }}>{l}</p>
            <p style={{ fontSize:18, fontFamily:"'Syne',sans-serif", fontWeight:800, color:"#fff" }}>{v}</p>
          </div>
        ))}
      </div>
      <div style={{ display:"grid", gridTemplateColumns:"repeat(2,1fr)", gap:7 }}>
        {TOOLS.slice(0,4).map((t,i) => (
          <div key={i} style={{ background:i===0?`${t.color}22`:"rgba(255,255,255,0.04)", border:`1px solid ${i===0?t.color+"60":"var(--border)"}`, borderRadius:9, padding:"9px 10px", display:"flex", alignItems:"center", gap:8 }}>
            <div style={{ width:26,height:26,borderRadius:6,background:t.grad,display:"flex",alignItems:"center",justifyContent:"center",flexShrink:0 }}><t.icon size={12} color="#fff"/></div>
            <span style={{ fontSize:11, color: i===0?"#F2F0FF":"#9896B8", fontWeight:500 }}>{t.label}</span>
          </div>
        ))}
      </div>
    </div>
  );

  if (stepId === "input") return (
    <div className="scale-in">
      <div style={{ display:"flex", alignItems:"center", gap:8, marginBottom:10 }}>
        <div style={{ width:28,height:28,borderRadius:7,background:"linear-gradient(135deg,#7C3AED,#EC4899)",display:"flex",alignItems:"center",justifyContent:"center" }}><RefreshCw size={13} color="#fff"/></div>
        <span style={{ fontSize:13, fontWeight:700 }}>Content Repurposer</span>
      </div>
      <p style={{ fontSize:10,color:"#9896B8",textTransform:"uppercase",letterSpacing:"0.07em",fontWeight:700,marginBottom:6 }}>Your Content / Topic</p>
      <div style={{ background:"rgba(124,58,237,0.08)", border:"1px solid rgba(124,58,237,0.4)", borderRadius:11, padding:"12px 14px", minHeight:72, boxShadow:"0 0 0 3px rgba(124,58,237,0.1)", marginBottom:10 }}>
        <span style={{ fontSize:13, color:"#F2F0FF", lineHeight:1.6 }}>{typed}</span>
        {!typeDone && <span className="cursor"/>}
      </div>
      <div style={{ display:"flex", gap:8, marginBottom:10 }}>
        {[["Tone","Professional"],["Audience","Content creators & founders"]].map(([l,v])=>(
          <div key={l} style={{ flex:l==="Tone"?1:2, background:"rgba(255,255,255,0.05)", borderRadius:9, padding:"8px 12px", border:"1px solid var(--border)" }}>
            <p style={{ fontSize:9,color:"#9896B8",textTransform:"uppercase",letterSpacing:"0.07em",marginBottom:2 }}>{l}</p>
            <p style={{ fontSize:12, color:"#F2F0FF", fontWeight:600 }}>{v}</p>
          </div>
        ))}
      </div>
      {typeDone && (
        <div className="fade-in" style={{ background:"linear-gradient(135deg,#7C3AED,#EC4899,#F97316)", backgroundSize:"200% 200%", animation:"gradShift 3s ease infinite", borderRadius:10, padding:"11px", textAlign:"center", fontSize:13, fontWeight:800, color:"#fff", display:"flex", alignItems:"center", justifyContent:"center", gap:6 }}>
          <Sparkles size={14}/> Generate Content
        </div>
      )}
    </div>
  );

  if (stepId === "generating") return (
    <div className="scale-in">
      <div style={{ textAlign:"center", paddingTop:10, marginBottom:18 }}>
        <div style={{ width:52,height:52,borderRadius:"50%",background:"linear-gradient(135deg,#7C3AED,#EC4899)",display:"flex",alignItems:"center",justifyContent:"center",margin:"0 auto 12px",boxShadow:"0 0 32px rgba(124,58,237,0.5)" }}>
          <Sparkles size={24} color="#fff" style={{ animation:"spin 2s linear infinite" }}/>
        </div>
        <p style={{ fontSize:14,fontWeight:700,marginBottom:3 }}>Generating your content pack…</p>
        <p style={{ fontSize:12,color:"#9896B8" }}>Claude AI is writing across 6 formats simultaneously</p>
      </div>
      <div style={{ marginBottom:14 }}>
        <div style={{ display:"flex",justifyContent:"space-between",marginBottom:5 }}>
          <span style={{ fontSize:11,color:"#9896B8" }}>Progress</span>
          <span style={{ fontSize:11,fontFamily:"'DM Mono',monospace",color:"#A78BFA" }}>{Math.round(gen)}%</span>
        </div>
        <div className="prog-track"><div className="prog-fill" style={{ width:`${gen}%`, background:"linear-gradient(90deg,#7C3AED,#EC4899,#F97316)", transition:"width .08s linear" }}/></div>
      </div>
      {[["🐦 Twitter/X Thread",gen>15,"#7C3AED"],["💼 LinkedIn Post",gen>35,"#6366F1"],["📸 Instagram Caption",gen>52,"#10B981"],["📧 Email Subject Lines",gen>68,"#F59E0B"],["🎬 Video Hook Script",gen>82,"#EF4444"],["📝 Blog Introduction",gen>94,"#EC4899"]].map(([l,done,c])=>(
        <div key={l} style={{ display:"flex",alignItems:"center",gap:8,padding:"7px 10px",background:done?`${c}12`:"rgba(255,255,255,0.03)",borderRadius:8,border:`1px solid ${done?c+"40":"var(--border)"}`,marginBottom:5 }}>
          {done ? <CheckCircle2 size={13} color={c}/> : <div style={{ width:13,height:13,borderRadius:"50%",border:"1.5px solid var(--text3)" }}/>}
          <span style={{ fontSize:12, color:done?"#F2F0FF":"#5E5C80" }}>{l}</span>
          {!done && gen>8 && <div style={{ marginLeft:"auto",width:60 }}><div className="shimmer-line"/></div>}
        </div>
      ))}
    </div>
  );

  if (stepId === "output") return (
    <div className="scale-in">
      <div style={{ display:"flex",alignItems:"center",justifyContent:"space-between",marginBottom:8 }}>
        <div style={{ display:"flex",alignItems:"center",gap:7 }}>
          <div style={{ width:8,height:8,borderRadius:"50%",background:"#10B981",boxShadow:"0 0 8px #10B981" }}/>
          <span style={{ fontSize:11,color:"#9896B8",fontWeight:700,textTransform:"uppercase",letterSpacing:"0.08em" }}>Output Ready</span>
        </div>
        <div style={{ display:"flex",alignItems:"center",gap:6,background:"rgba(16,185,129,0.1)",border:"1px solid rgba(16,185,129,0.3)",borderRadius:8,padding:"4px 10px",fontSize:11,color:"#5EEAD4",fontWeight:700 }}>
          <Copy size={11}/>Copy All
        </div>
      </div>
      <div style={{ background:"rgba(255,255,255,0.03)",border:"1px solid var(--border-bright)",borderRadius:11,padding:"12px 14px",maxHeight:230,overflowY:"auto",fontSize:12.5,lineHeight:1.75 }}>
        {DEMO_OUTPUT.split('\n').slice(0,outLines).map((line,i)=>{
          const bold=/^[🐦💼📸📧🎬📝✅🏷️⭐]/.test(line)||line.startsWith('#');
          return <p key={i} style={{ marginBottom:line===''?8:3, color:bold?"#F2F0FF":"#9896B8", fontWeight:bold?700:400 }}>{line||'\u00A0'}</p>;
        })}
        {outLines < DEMO_OUTPUT.split('\n').length && <span className="cursor"/>}
      </div>
    </div>
  );

  if (stepId === "tools") return (
    <div className="scale-in">
      <p style={{ fontSize:10,color:"#9896B8",textTransform:"uppercase",letterSpacing:"0.08em",fontWeight:700,marginBottom:10 }}>All Eight Tools Included</p>
      <div style={{ display:"grid", gridTemplateColumns:"repeat(2,1fr)", gap:7 }}>
        {TOOLS.map((t,i)=>(
          <div key={i} className="fade-in" style={{ display:"flex",alignItems:"center",gap:9,padding:"9px 11px",background:`${t.color}0F`,borderRadius:9,border:`1px solid ${t.color}35`,animationDelay:`${i*0.06}s` }}>
            <div style={{ width:28,height:28,borderRadius:7,background:t.grad,display:"flex",alignItems:"center",justifyContent:"center",flexShrink:0 }}><t.icon size={13} color="#fff"/></div>
            <span style={{ fontSize:11,fontWeight:600,color:"#F2F0FF",lineHeight:1.2 }}>{t.label}</span>
            <CheckCircle2 size={12} color="#10B981" style={{ marginLeft:"auto",flexShrink:0 }}/>
          </div>
        ))}
      </div>
      <div style={{ marginTop:12,background:"linear-gradient(135deg,rgba(124,58,237,0.15),rgba(236,72,153,0.1))",border:"1px solid rgba(124,58,237,0.35)",borderRadius:11,padding:"11px 14px",textAlign:"center" }}>
        <p style={{ fontSize:13,fontWeight:700,color:"#A78BFA" }}>🎉 Start free — no credit card needed</p>
        <p style={{ fontSize:11,color:"#9896B8",marginTop:2 }}>First 10 generations free · Pro from $49/mo</p>
      </div>
    </div>
  );

  return null;
}

/* ══════════════════════════════════════════════════════════════════
   DEMO MODAL
══════════════════════════════════════════════════════════════════ */
function DemoModal({ onClose, onStartApp }) {
  const [step, setStep] = useState(0);
  const [paused, setPaused] = useState(false);
  const [elapsed, setElapsed] = useState(0);
  const timerRef = useRef(null);
  const current = DEMO_STEPS[step];
  const isLast = step === DEMO_STEPS.length - 1;

  const clearTimer = useCallback(() => {
    if (timerRef.current) { clearInterval(timerRef.current); timerRef.current = null; }
  }, []);

  const goTo = useCallback((target) => {
    clearTimer();
    setElapsed(0);
    setStep(typeof target === "function" ? target : () => target);
  }, [clearTimer]);

  const goBack = useCallback(() => goTo(s => Math.max(s - 1, 0)), [goTo]);

  useEffect(() => {
    clearTimer(); setElapsed(0);
    if (paused || isLast) return;
    timerRef.current = setInterval(() => {
      setElapsed(e => {
        if (e + 100 >= current.duration) {
          clearTimer();
          setStep(s => Math.min(s + 1, DEMO_STEPS.length - 1));
          return 0;
        }
        return e + 100;
      });
    }, 100);
    return clearTimer;
  }, [step, paused, isLast, current.duration, clearTimer]);

  const pct = Math.min((elapsed / current.duration) * 100, 100);

  const STEP_COLORS = ["#7C3AED","#6366F1","#10B981","#F59E0B","#EC4899"];

  return (
    <div className="demo-overlay" onClick={e => e.target === e.currentTarget && onClose()}>
      <div className="demo-modal">
        {/* Top bar */}
        <div style={{ padding:"20px 24px 0", display:"flex", alignItems:"center", justifyContent:"space-between", flexShrink:0 }}>
          <div style={{ display:"flex", alignItems:"center", gap:10 }}>
            <div style={{ width:32,height:32,borderRadius:9,background:"linear-gradient(135deg,#7C3AED,#EC4899)",display:"flex",alignItems:"center",justifyContent:"center",boxShadow:"0 4px 16px rgba(124,58,237,0.5)" }}><Sparkles size={15} color="#fff"/></div>
            <span style={{ fontFamily:"'Syne',sans-serif", fontSize:16, fontWeight:800 }}>ContentForge AI</span>
            <span style={{ background:"linear-gradient(135deg,rgba(124,58,237,0.2),rgba(236,72,153,0.15))", border:"1px solid rgba(124,58,237,0.4)", borderRadius:20, padding:"3px 12px", fontSize:10, color:"#A78BFA", fontWeight:800, letterSpacing:"0.07em" }}>LIVE DEMO</span>
          </div>
          <button onClick={onClose} style={{ width:32,height:32,borderRadius:"50%",background:"rgba(255,255,255,0.07)",border:"1px solid var(--border-bright)",cursor:"pointer",display:"flex",alignItems:"center",justifyContent:"center",color:"#9896B8" }}>
            <X size={15}/>
          </button>
        </div>

        {/* Step progress dots */}
        <div style={{ padding:"16px 24px 0", display:"flex", gap:6 }}>
          {DEMO_STEPS.map((_,i) => (
            <button key={i} onClick={() => goTo(i)} style={{ flex:1,height:4,borderRadius:2,border:"none",cursor:"pointer",padding:0,
              background: i < step ? STEP_COLORS[i] : i === step ? `linear-gradient(90deg,${STEP_COLORS[i]} ${pct}%,rgba(255,255,255,0.1) ${pct}%)` : "rgba(255,255,255,0.1)",
              transition:"background 0.12s",
            }}/>
          ))}
        </div>

        {/* Body */}
        <div style={{ padding:"20px 24px", flex:1, overflowY:"auto" }}>
          <div style={{ display:"flex", gap:22, flexWrap:"wrap" }}>
            {/* Narrative */}
            <div style={{ flex:"1 1 230px" }}>
              <div style={{ display:"inline-flex",alignItems:"center",gap:6,padding:"4px 12px",borderRadius:20,background:`${STEP_COLORS[step]}20`,border:`1px solid ${STEP_COLORS[step]}50`,fontSize:10,color:STEP_COLORS[step],fontWeight:800,letterSpacing:"0.08em",marginBottom:12 }}>
                {current.label}
              </div>
              <h2 key={`t-${step}`} className="slide-in" style={{ fontFamily:"'Syne',sans-serif", fontSize:20, fontWeight:800, marginBottom:8, lineHeight:1.2 }}>{current.title}</h2>
              <p key={`s-${step}`} className="slide-in" style={{ color:"#9896B8", fontSize:13, lineHeight:1.7, marginBottom:16, animationDelay:"0.06s" }}>{current.subtitle}</p>
              <div key={`n-${step}`} className="slide-in" style={{ background:"rgba(255,255,255,0.04)",border:"1px solid var(--border-bright)",borderRadius:12,padding:"14px 16px",animationDelay:"0.12s" }}>
                <p style={{ fontSize:12, color:"#9896B8", lineHeight:1.8, fontStyle:"italic" }}>"{current.narration}"</p>
              </div>
            </div>
            {/* Screen */}
            <div style={{ flex:"1 1 300px" }}>
              <div className="demo-screen" key={`screen-${step}`}>
                <DemoScreen stepId={current.screen}/>
              </div>
            </div>
          </div>
        </div>

        {/* Footer controls */}
        <div style={{ padding:"14px 24px 20px", borderTop:"1px solid var(--border)", display:"flex", alignItems:"center", justifyContent:"space-between", gap:10, flexShrink:0 }}>
          <button className="demo-nav-btn" onClick={goBack}
            style={{ background:"rgba(255,255,255,0.05)",border:"1px solid var(--border-bright)",color:step===0?"#5E5C80":"#9896B8", opacity:step===0?0.4:1, pointerEvents:step===0?"none":"auto", cursor:step===0?"default":"pointer" }}>
            <ChevronLeft size={15}/> Back
          </button>
          <button className="demo-nav-btn" onClick={() => setPaused(p => !p)}
            style={{ background:"rgba(255,255,255,0.05)",border:"1px solid var(--border-bright)",color:"#9896B8" }}>
            {paused ? <Play size={14}/> : <Pause size={14}/>}
            {paused ? "Resume" : "Pause"}
          </button>
          {isLast ? (
            <button className="demo-nav-btn grad-btn" onClick={onStartApp}
              style={{ color:"#fff", boxShadow:"0 6px 24px rgba(124,58,237,0.45)", padding:"10px 22px" }}>
              <Sparkles size={14}/> Start Free Trial
            </button>
          ) : (
            <button className="demo-nav-btn grad-btn" onClick={() => goTo(s => Math.min(s + 1, DEMO_STEPS.length - 1))}
              style={{ color:"#fff", boxShadow:"0 6px 24px rgba(124,58,237,0.4)", padding:"10px 22px" }}>
              Next <ChevronRight size={15}/>
            </button>
          )}
        </div>
      </div>
    </div>
  );
}

/* ══════════════════════════════════════════════════════════════════
   TOOL PANEL (APP)
══════════════════════════════════════════════════════════════════ */
function ToolPanel({ toolId, toolInfo }) {
  const [input, setInput] = useState("");
  const [tone, setTone] = useState("professional");
  const [audience, setAudience] = useState("");
  const [loading, setLoading] = useState(false);
  const [output, setOutput] = useState("");
  const [err, setErr] = useState("");
  const PLACEHOLDERS = {
    repurpose:"Paste your blog post, article, or podcast transcript here...",
    blog:"Describe your blog topic, audience, and the main point to convey...",
    social:"Describe your product, offer, achievement, or story to share...",
    email:"Describe your product/service, offer, and target customer pain points...",
    youtube:"Describe your video topic, audience, and 3 key points to cover...",
    seo:"Describe your page or product — what it's about and who it's for...",
    ads:"Describe your product/service, unique value proposition, and target customer...",
    product:"Describe your product — features, materials, use case, target buyer...",
  };
  const handle = async () => {
    if (!input.trim()) { setErr("Please enter your content first."); return; }
    setErr(""); setLoading(true); setOutput("");
    try {
      setOutput(await callClaude(toolId, `Topic/Content: ${input}\nTone: ${tone}${audience ? `\nTarget Audience: ${audience}` : ""}`));
    } catch { setErr("Generation failed. Check your connection and try again."); }
    setLoading(false);
  };
  return (
    <div>
      {/* Header */}
      <div style={{ display:"flex", alignItems:"center", gap:14, marginBottom:28, padding:"18px 20px", background:`${toolInfo.color}0D`, border:`1px solid ${toolInfo.color}30`, borderRadius:16 }}>
        <div style={{ width:48,height:48,borderRadius:13,background:toolInfo.grad,display:"flex",alignItems:"center",justifyContent:"center",boxShadow:`0 6px 20px ${toolInfo.color}55`,flexShrink:0 }}>
          <toolInfo.icon size={22} color="#fff"/>
        </div>
        <div>
          <h2 style={{ fontSize:20,fontFamily:"'Syne',sans-serif",fontWeight:800,marginBottom:2 }}>{toolInfo.label}</h2>
          <p style={{ color:"#9896B8",fontSize:13 }}>{toolInfo.desc}</p>
        </div>
      </div>

      {/* Input */}
      <div style={{ marginBottom:14 }}>
        <label style={{ display:"block",fontSize:11,fontWeight:700,color:"#9896B8",textTransform:"uppercase",letterSpacing:"0.08em",marginBottom:8 }}>Your Content / Topic</label>
        <textarea value={input} onChange={e => setInput(e.target.value)} placeholder={PLACEHOLDERS[toolId] || "Enter your topic..."}/>
      </div>

      {/* Options */}
      <div style={{ display:"flex", gap:12, marginBottom:20, flexWrap:"wrap" }}>
        <div style={{ flex:1, minWidth:130 }}>
          <label style={{ display:"block",fontSize:11,fontWeight:700,color:"#9896B8",textTransform:"uppercase",letterSpacing:"0.08em",marginBottom:7 }}>Tone</label>
          <select value={tone} onChange={e => setTone(e.target.value)}>
            {["professional","casual","witty","authoritative","empathetic","urgent","inspirational","educational"].map(t =>
              <option key={t} value={t}>{t.charAt(0).toUpperCase()+t.slice(1)}</option>
            )}
          </select>
        </div>
        <div style={{ flex:2, minWidth:200 }}>
          <label style={{ display:"block",fontSize:11,fontWeight:700,color:"#9896B8",textTransform:"uppercase",letterSpacing:"0.08em",marginBottom:7 }}>Target Audience (optional)</label>
          <input value={audience} onChange={e => setAudience(e.target.value)} placeholder="e.g. SaaS founders, fitness beginners, busy moms..."/>
        </div>
      </div>

      {err && (
        <div style={{ display:"flex",alignItems:"center",gap:8,padding:"11px 14px",background:"rgba(244,63,94,0.1)",border:"1px solid rgba(244,63,94,0.35)",borderRadius:11,marginBottom:16,color:"#FB7185",fontSize:13 }}>
          <AlertCircle size={14}/> {err}
        </div>
      )}

      <button onClick={handle} disabled={loading} className={loading ? "" : "grad-btn"}
        style={{ width:"100%",padding:"15px 24px",borderRadius:13,border:"none",
          background:loading?"rgba(255,255,255,0.06)":undefined,
          color:loading?"#5E5C80":"#fff",
          fontSize:14,fontWeight:800,cursor:loading?"not-allowed":"pointer",
          display:"flex",alignItems:"center",justifyContent:"center",gap:8,
          boxShadow:loading?"none":undefined,
        }}>
        {loading ? <><Loader2 size={16} className="spin-anim"/>Generating with Claude AI…</> : <><Sparkles size={16}/>Generate Content</>}
      </button>

      {output && (
        <div style={{ marginTop:26 }}>
          <div style={{ display:"flex",alignItems:"center",justifyContent:"space-between",marginBottom:12 }}>
            <div style={{ display:"flex",alignItems:"center",gap:8 }}>
              <div style={{ width:8,height:8,borderRadius:"50%",background:"#10B981",boxShadow:"0 0 10px #10B981" }}/>
              <span style={{ fontSize:11,color:"#9896B8",fontWeight:700,textTransform:"uppercase",letterSpacing:"0.08em" }}>Generated Output</span>
            </div>
            <CopyBtn text={output}/>
          </div>
          <div className="output-block">
            {output.split('\n').map((line, i) => {
              const bold = /^[🐦💼📸📧🎬📝🔍📘✅🏷️⭐🎯📋🔗📊🌟🎣📣EMAIL BLOG GOOGLE TWITTER LINKEDIN INSTAGRAM]/u.test(line);
              return <p key={i} style={{ marginBottom:line===''?10:4, color:bold?"#F2F0FF":"#9896B8", fontWeight:bold?700:400, fontSize:bold?14:13.5 }}>{line||'\u00A0'}</p>;
            })}
          </div>
        </div>
      )}
    </div>
  );
}

/* ══════════════════════════════════════════════════════════════════
   DASHBOARD HOME
══════════════════════════════════════════════════════════════════ */
function DashboardHome({ onSelectTool }) {
  const weekly = [{d:"Mon",v:24},{d:"Tue",v:38},{d:"Wed",v:19},{d:"Thu",v:52},{d:"Fri",v:45},{d:"Sat",v:31},{d:"Sun",v:11}];
  const mx = Math.max(...weekly.map(x => x.v));
  const weekGrads = ["#7C3AED","#6366F1","#06B6D4","#10B981","#F59E0B","#F97316","#EC4899"];
  const userStats = [
    { label:"Generations Used",  val:"219",    pct:44,  grad:"linear-gradient(135deg,#7C3AED,#A78BFA)", track:"rgba(124,58,237,0.15)" },
    { label:"Words Generated",   val:"84,200", pct:null, grad:"linear-gradient(135deg,#06B6D4,#6366F1)", track:null },
    { label:"Hours Saved",       val:"31 hrs", pct:null, grad:"linear-gradient(135deg,#10B981,#84CC16)", track:null },
    { label:"Days to Renewal",   val:"12",     pct:40,  grad:"linear-gradient(135deg,#F59E0B,#F97316)", track:"rgba(245,158,11,0.15)" },
  ];
  return (
    <div>
      <div style={{ marginBottom:28 }}>
        <h1 style={{ fontFamily:"'Syne',sans-serif", fontSize:26, fontWeight:800, marginBottom:4 }}>Good morning, Creator 👋</h1>
        <p style={{ color:"#9896B8" }}>You're on the <span style={{ background:"linear-gradient(135deg,#7C3AED,#EC4899)", WebkitBackgroundClip:"text", WebkitTextFillColor:"transparent", fontWeight:700 }}>Pro Plan</span>. 281 generations remaining this cycle.</p>
      </div>

      {/* User stats */}
      <div style={{ display:"grid", gridTemplateColumns:"repeat(auto-fit,minmax(170px,1fr))", gap:12, marginBottom:24 }}>
        {userStats.map((s,i) => (
          <div key={i} className="stat-card" style={{ background:`${s.grad.match(/#[0-9A-F]{6}/i)?.[0]}12` || "rgba(255,255,255,0.04)", border:`1px solid ${s.grad.match(/#[0-9A-F]{6}/i)?.[0]}35` || "var(--border)" }}>
            <p style={{ fontSize:10,color:"#9896B8",textTransform:"uppercase",letterSpacing:"0.08em",fontWeight:700,marginBottom:8 }}>{s.label}</p>
            <p style={{ fontSize:24,fontFamily:"'Syne',sans-serif",fontWeight:800,marginBottom:s.pct?10:0,background:s.grad,WebkitBackgroundClip:"text",WebkitTextFillColor:"transparent" }}>{s.val}</p>
            {s.pct && <div className="prog-track" style={{ background:s.track }}><div className="prog-fill" style={{ width:`${s.pct}%`, background:s.grad }}/></div>}
          </div>
        ))}
      </div>

      {/* Chart */}
      <div style={{ background:"rgba(255,255,255,0.03)", border:"1px solid var(--border-bright)", borderRadius:18, padding:22, marginBottom:24 }}>
        <p style={{ fontSize:11,fontWeight:700,color:"#9896B8",textTransform:"uppercase",letterSpacing:"0.08em",marginBottom:18 }}>Generations This Week</p>
        <div style={{ display:"flex", alignItems:"flex-end", gap:10, height:100 }}>
          {weekly.map((d,i) => (
            <div key={i} style={{ flex:1, display:"flex", flexDirection:"column", alignItems:"center", gap:5 }}>
              <span style={{ fontSize:9,color:"#9896B8" }} className="mono">{d.v}</span>
              <div style={{ width:"100%",borderRadius:"6px 6px 3px 3px",height:`${(d.v/mx)*82}px`,background:d.d==="Thu"?`linear-gradient(180deg,${weekGrads[i]},${weekGrads[(i+2)%7]})`:`${weekGrads[i]}35`,minHeight:4,transition:"height .4s ease" }}/>
              <span style={{ fontSize:9,color:"#5E5C80" }}>{d.d}</span>
            </div>
          ))}
        </div>
      </div>

      {/* Quick start */}
      <p style={{ fontSize:11,fontWeight:700,color:"#9896B8",textTransform:"uppercase",letterSpacing:"0.08em",marginBottom:14 }}>Quick Start</p>
      <div style={{ display:"grid", gridTemplateColumns:"repeat(auto-fill,minmax(220px,1fr))", gap:11 }}>
        {TOOLS.map(t => (
          <button key={t.id} className="tool-btn" onClick={() => onSelectTool(t.id)}
            style={{ background:`${t.color}0A`, border:`1px solid ${t.color}30` }}>
            <div style={{ display:"flex",alignItems:"center",gap:10,marginBottom:7 }}>
              <div style={{ width:34,height:34,borderRadius:9,background:t.grad,display:"flex",alignItems:"center",justifyContent:"center",boxShadow:`0 4px 12px ${t.color}45` }}><t.icon size={16} color="#fff"/></div>
              <span style={{ fontSize:13,fontWeight:700,color:"#F2F0FF" }}>{t.label}</span>
            </div>
            <p style={{ fontSize:11,color:"#9896B8",paddingLeft:44 }}>{t.desc}</p>
          </button>
        ))}
      </div>
    </div>
  );
}

/* ══════════════════════════════════════════════════════════════════
   APP DASHBOARD SHELL
══════════════════════════════════════════════════════════════════ */
function AppDashboard({ onSignOut }) {
  const [page, setPage] = useState("dashboard");
  const tool = TOOLS.find(t => t.id === page);
  return (
    <div style={{ display:"flex", minHeight:"100vh" }}>
      <style>{CSS}</style>
      {/* Sidebar */}
      <nav className="sidebar">
        {/* Logo */}
        <div style={{ padding:"22px 16px 18px", borderBottom:"1px solid var(--border)" }}>
          <div style={{ display:"flex",alignItems:"center",gap:10 }}>
            <div style={{ width:34,height:34,borderRadius:9,background:"linear-gradient(135deg,#7C3AED,#EC4899,#F97316)",backgroundSize:"200% 200%",animation:"gradShift 4s ease infinite",display:"flex",alignItems:"center",justifyContent:"center",boxShadow:"0 4px 16px rgba(124,58,237,0.5)" }}><Sparkles size={17} color="#fff"/></div>
            <span style={{ fontFamily:"'Syne',sans-serif",fontSize:17,fontWeight:800 }}>ContentForge</span>
          </div>
        </div>
        {/* Nav */}
        <div style={{ flex:1,padding:"14px 10px",overflowY:"auto" }}>
          {NAV.map(n => (
            <button key={n.id} onClick={() => setPage(n.id)} className={`nav-item ${page===n.id?"active":""}`}>
              <div style={{ width:26,height:26,borderRadius:7,background:page===n.id?n.grad:"rgba(255,255,255,0.06)",display:"flex",alignItems:"center",justifyContent:"center",flexShrink:0,transition:"background .18s" }}><n.icon size={13} color={page===n.id?"#fff":"#9896B8"}/></div>
              {n.label}
            </button>
          ))}
        </div>
        {/* Plan badge */}
        <div style={{ padding:"14px 14px 18px",borderTop:"1px solid var(--border)" }}>
          <div style={{ background:"linear-gradient(135deg,rgba(124,58,237,0.15),rgba(236,72,153,0.1))",border:"1px solid rgba(124,58,237,0.35)",borderRadius:12,padding:"12px 14px",marginBottom:10 }}>
            <p style={{ fontSize:10,fontWeight:800,letterSpacing:"0.07em",textTransform:"uppercase",marginBottom:2,background:"linear-gradient(135deg,#A78BFA,#F9A8D4)",WebkitBackgroundClip:"text",WebkitTextFillColor:"transparent" }}>Pro Plan</p>
            <p style={{ fontSize:11,color:"#9896B8",marginBottom:7 }}>281 / 500 remaining</p>
            <div className="prog-track"><div className="prog-fill" style={{ width:"44%",background:"linear-gradient(90deg,#7C3AED,#EC4899)" }}/></div>
          </div>
          <button onClick={onSignOut} style={{ display:"flex",alignItems:"center",gap:8,width:"100%",padding:"8px 10px",background:"none",border:"none",color:"#9896B8",fontSize:13,cursor:"pointer",borderRadius:9,fontFamily:"'Plus Jakarta Sans',sans-serif",transition:"color .15s" }}>
            <LogOut size={14}/>Sign Out
          </button>
        </div>
      </nav>

      {/* Main */}
      <main className="main-c" style={{ flex:1 }}>
        {/* Topbar */}
        <div style={{ background:"rgba(8,8,18,0.9)",backdropFilter:"blur(24px)",borderBottom:"1px solid var(--border)",padding:"0 26px",height:62,display:"flex",alignItems:"center",justifyContent:"space-between",position:"sticky",top:0,zIndex:30 }}>
          <div style={{ display:"flex",alignItems:"center",gap:8 }}>
            <Sparkles size={14} color="#A78BFA"/>
            <span style={{ fontSize:13,color:"#9896B8" }}>Powered by <span style={{ color:"#A78BFA",fontWeight:600 }}>Claude AI</span></span>
          </div>
          <div style={{ width:34,height:34,borderRadius:"50%",background:"linear-gradient(135deg,#7C3AED,#EC4899)",display:"flex",alignItems:"center",justifyContent:"center",fontSize:13,fontWeight:800,color:"#fff",boxShadow:"0 4px 14px rgba(124,58,237,0.45)" }}>P</div>
        </div>
        <div style={{ padding:"28px 28px 70px" }}>
          {page==="dashboard" ? <DashboardHome onSelectTool={setPage}/> : tool ? <ToolPanel toolId={tool.id} toolInfo={tool}/> : null}
        </div>
      </main>
    </div>
  );
}

/* ══════════════════════════════════════════════════════════════════
   LANDING PAGE
══════════════════════════════════════════════════════════════════ */
function LandingPage({ onEnter }) {
  const [scrolled, setScrolled] = useState(false);
  const [showDemo, setShowDemo] = useState(false);
  useEffect(() => {
    const fn = () => setScrolled(window.scrollY > 40);
    window.addEventListener("scroll", fn);
    return () => window.removeEventListener("scroll", fn);
  }, []);

  const TICKER_ITEMS = ["Content Repurposer","Blog Writer","Social Media Pack","Email Campaigns","YouTube Scripts","SEO Meta Generator","Ad Copy Creator","Product Descriptions","AI-Powered","Save 20+ Hours/Week"];

  return (
    <div style={{ minHeight:"100vh", background:"var(--bg)" }}>
      <style>{CSS}</style>
      {showDemo && <DemoModal onClose={() => setShowDemo(false)} onStartApp={() => { setShowDemo(false); onEnter(); }}/>}

      {/* ── NAV ── */}
      <header style={{ position:"fixed",top:0,left:0,right:0,zIndex:50,background:scrolled?"rgba(8,8,18,0.92)":"transparent",backdropFilter:scrolled?"blur(20px)":"none",borderBottom:scrolled?"1px solid var(--border)":"1px solid transparent",transition:"all .3s" }}>
        <div style={{ maxWidth:1200,margin:"0 auto",padding:"0 30px",height:68,display:"flex",alignItems:"center",justifyContent:"space-between" }}>
          <div style={{ display:"flex",alignItems:"center",gap:10 }}>
            <div style={{ width:36,height:36,borderRadius:10,background:"linear-gradient(135deg,#7C3AED,#EC4899,#F97316)",backgroundSize:"200% 200%",animation:"gradShift 4s ease infinite",display:"flex",alignItems:"center",justifyContent:"center",boxShadow:"0 4px 16px rgba(124,58,237,0.4)" }}><Sparkles size={18} color="#fff"/></div>
            <span style={{ fontFamily:"'Syne',sans-serif",fontSize:19,fontWeight:800 }}>ContentForge AI</span>
          </div>
          <div style={{ display:"flex",gap:28,alignItems:"center" }}>
            {["Features","Pricing","Testimonials"].map(i => (
              <a key={i} href="#" style={{ color:"#9896B8",fontSize:14,textDecoration:"none",transition:"color .2s" }}
                onMouseEnter={e=>e.target.style.color="#F2F0FF"} onMouseLeave={e=>e.target.style.color="#9896B8"}>{i}</a>
            ))}
            <button onClick={onEnter} className="grad-btn" style={{ borderRadius:11,padding:"10px 22px",color:"#fff",fontSize:13,fontWeight:800,boxShadow:"0 4px 18px rgba(124,58,237,0.4)" }}>
              Start Free →
            </button>
          </div>
        </div>
      </header>

      {/* ── HERO ── */}
      <section style={{ minHeight:"100vh",display:"flex",alignItems:"center",justifyContent:"center",textAlign:"center",padding:"130px 28px 80px",position:"relative",overflow:"hidden" }}>
        {/* Colour orbs */}
        <div className="orb" style={{ width:600,height:600,background:"radial-gradient(circle,rgba(124,58,237,0.18),transparent 70%)",top:"-10%",left:"-5%",animation:"float1 12s ease-in-out infinite" }}/>
        <div className="orb" style={{ width:500,height:500,background:"radial-gradient(circle,rgba(236,72,153,0.14),transparent 70%)",top:"10%",right:"-8%",animation:"float2 14s ease-in-out infinite" }}/>
        <div className="orb" style={{ width:400,height:400,background:"radial-gradient(circle,rgba(6,182,212,0.12),transparent 70%)",bottom:"5%",left:"20%",animation:"float3 10s ease-in-out infinite" }}/>
        <div className="orb" style={{ width:300,height:300,background:"radial-gradient(circle,rgba(245,158,11,0.1),transparent 70%)",bottom:"15%",right:"10%",animation:"float1 16s ease-in-out infinite" }}/>
        {/* Grid */}
        <div style={{ position:"absolute",inset:0,backgroundImage:"linear-gradient(rgba(255,255,255,0.025) 1px,transparent 1px),linear-gradient(90deg,rgba(255,255,255,0.025) 1px,transparent 1px)",backgroundSize:"48px 48px",opacity:.7 }}/>

        <div style={{ maxWidth:840,position:"relative",zIndex:2 }}>
          {/* Badge */}
          <div className="fu1" style={{ display:"inline-flex",alignItems:"center",gap:8,padding:"7px 18px",background:"linear-gradient(135deg,rgba(124,58,237,0.15),rgba(236,72,153,0.1))",border:"1px solid rgba(124,58,237,0.35)",borderRadius:24,marginBottom:30 }}>
            <Flame size={13} color="#F97316"/><span style={{ fontSize:11,fontWeight:800,letterSpacing:"0.08em",background:"linear-gradient(135deg,#A78BFA,#F9A8D4)",WebkitBackgroundClip:"text",WebkitTextFillColor:"transparent" }}>AI-POWERED CONTENT ENGINE — 2026</span>
          </div>

          <h1 className="fu2" style={{ fontFamily:"'Syne',sans-serif",fontSize:"clamp(44px,7.5vw,88px)",fontWeight:800,lineHeight:1.05,letterSpacing:"-0.03em",marginBottom:26 }}>
            Turn One Idea Into<br/><span className="g-hero">30 Days of Content</span>
          </h1>

          <p className="fu3" style={{ fontSize:18,color:"#9896B8",maxWidth:560,margin:"0 auto 44px",lineHeight:1.75,fontWeight:400 }}>
            ContentForge AI transforms a single piece of content into blog posts, social packs, email sequences, YouTube scripts, and more — in under 30 seconds.
          </p>

          <div className="fu4" style={{ display:"flex",gap:14,justifyContent:"center",flexWrap:"wrap" }}>
            <button onClick={onEnter} className="grad-btn" style={{ display:"flex",alignItems:"center",gap:10,borderRadius:14,padding:"17px 34px",color:"#fff",fontSize:15,fontWeight:800,boxShadow:"0 10px 40px rgba(124,58,237,0.45)" }}>
              <Rocket size={18}/> Launch Free App <ArrowRight size={16}/>
            </button>
            <button onClick={() => setShowDemo(true)}
              style={{ display:"flex",alignItems:"center",gap:9,background:"rgba(255,255,255,0.06)",border:"1px solid var(--border-bright)",borderRadius:14,padding:"17px 30px",color:"#F2F0FF",fontSize:15,fontWeight:600,cursor:"pointer",transition:"all .22s" }}
              onMouseEnter={e=>{e.currentTarget.style.background="rgba(255,255,255,0.1)";e.currentTarget.style.borderColor="rgba(255,255,255,0.3)";}}
              onMouseLeave={e=>{e.currentTarget.style.background="rgba(255,255,255,0.06)";e.currentTarget.style.borderColor="var(--border-bright)";}}>
              <Eye size={16}/> Watch Demo
            </button>
          </div>

          {/* Trust pills */}
          <div className="fu5" style={{ display:"flex",gap:20,justifyContent:"center",marginTop:44,flexWrap:"wrap" }}>
            {[["No credit card",Shield,"#7C3AED"],["60-sec setup",Zap,"#F59E0B"],["Cancel anytime",Check,"#10B981"]].map(([t,I,c])=>(
              <div key={t} style={{ display:"flex",alignItems:"center",gap:6,fontSize:13,color:"#5E5C80" }}>
                <div style={{ width:20,height:20,borderRadius:"50%",background:`${c}20`,display:"flex",alignItems:"center",justifyContent:"center" }}><I size={11} color={c}/></div>{t}
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* ── TICKER TAPE ── */}
      <div className="ticker-wrap">
        <div className="ticker-inner">
          {[...TICKER_ITEMS,...TICKER_ITEMS].map((item,i)=>(
            <span key={i} style={{ fontSize:12,fontWeight:700,letterSpacing:"0.06em",color:["#A78BFA","#F9A8D4","#67E8F9","#BEF264","#FCD34D","#FB7185","#FB923C","#5EEAD4","#818CF8","#F9A8D4"][i%10] }}>
              ✦ {item.toUpperCase()}
            </span>
          ))}
        </div>
      </div>

      {/* ── GLOBAL STATS ── */}
      <section style={{ padding:"70px 28px",background:"var(--bg2)",borderBottom:"1px solid var(--border)" }}>
        <div style={{ maxWidth:1100,margin:"0 auto",display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(220px,1fr))",gap:16 }}>
          {STATS_GLOBAL.map((s,i)=>(
            <div key={i} style={{ background:s.grad.replace("linear-gradient(135deg,","linear-gradient(135deg,").replace(",",",").split(",").slice(0,1).join(",")+`12,transparent)`.replace("linear-gradient(135deg,","").split(",")[0].trim()+"14", borderRadius:18, padding:24, border:`1px solid ${s.glow.replace("0.25","0.3")}`, textAlign:"center",
              background:`linear-gradient(135deg,${s.glow} 0%,rgba(255,255,255,0.02) 100%)`,
            }}>
              <div style={{ width:48,height:48,borderRadius:13,background:s.grad,display:"flex",alignItems:"center",justifyContent:"center",margin:"0 auto 14px",boxShadow:`0 6px 20px ${s.glow}` }}><s.icon size={22} color="#fff"/></div>
              <p style={{ fontFamily:"'Syne',sans-serif",fontSize:36,fontWeight:800,background:s.grad,WebkitBackgroundClip:"text",WebkitTextFillColor:"transparent" }}>{s.v}</p>
              <p style={{ fontSize:13,color:"#9896B8",marginTop:3,fontWeight:500 }}>{s.label}</p>
            </div>
          ))}
        </div>
      </section>

      {/* ── FEATURES GRID ── */}
      <section style={{ padding:"100px 28px",maxWidth:1200,margin:"0 auto" }}>
        <div style={{ textAlign:"center",marginBottom:60 }}>
          <p style={{ fontSize:11,fontWeight:800,letterSpacing:"0.12em",textTransform:"uppercase",marginBottom:14,background:"linear-gradient(135deg,#A78BFA,#F9A8D4)",WebkitBackgroundClip:"text",WebkitTextFillColor:"transparent" }}>THE FULL TOOLKIT</p>
          <h2 style={{ fontFamily:"'Syne',sans-serif",fontSize:"clamp(30px,5vw,54px)",fontWeight:800,marginBottom:14 }}>
            Eight AI tools.<br/><span style={{ fontWeight:400,fontStyle:"italic",color:"#9896B8" }}>One subscription.</span>
          </h2>
          <p style={{ color:"#9896B8",maxWidth:480,margin:"0 auto",lineHeight:1.7 }}>Every content format your business needs, powered by the world's most capable AI.</p>
        </div>
        <div style={{ display:"grid",gridTemplateColumns:"repeat(auto-fill,minmax(258px,1fr))",gap:14 }}>
          {TOOLS.map(t => (
            <div key={t.id} className="feat-card" style={{ background:`${t.color}0A`,border:`1px solid ${t.color}28` }}
              onMouseEnter={e=>{ e.currentTarget.style.borderColor=`${t.color}60`; e.currentTarget.style.background=`${t.color}14`; }}
              onMouseLeave={e=>{ e.currentTarget.style.borderColor=`${t.color}28`; e.currentTarget.style.background=`${t.color}0A`; }}>
              <div style={{ width:46,height:46,borderRadius:12,background:t.grad,display:"flex",alignItems:"center",justifyContent:"center",marginBottom:16,boxShadow:`0 6px 20px ${t.color}50` }}><t.icon size={21} color="#fff"/></div>
              <h3 style={{ fontSize:15,fontWeight:700,marginBottom:7,fontFamily:"'Syne',sans-serif" }}>{t.label}</h3>
              <p style={{ fontSize:13,color:"#9896B8",lineHeight:1.65 }}>{t.desc}</p>
            </div>
          ))}
        </div>
      </section>

      {/* ── HOW IT WORKS ── */}
      <section style={{ padding:"80px 28px",background:"var(--bg2)",borderTop:"1px solid var(--border)",borderBottom:"1px solid var(--border)" }}>
        <div style={{ maxWidth:960,margin:"0 auto",textAlign:"center" }}>
          <p style={{ fontSize:11,fontWeight:800,letterSpacing:"0.12em",textTransform:"uppercase",marginBottom:14,background:"linear-gradient(135deg,#67E8F9,#818CF8)",WebkitBackgroundClip:"text",WebkitTextFillColor:"transparent" }}>HOW IT WORKS</p>
          <h2 style={{ fontFamily:"'Syne',sans-serif",fontSize:"clamp(28px,4vw,48px)",fontWeight:800,marginBottom:56 }}>
            Three steps to <span style={{ color:"#9896B8",fontWeight:400,fontStyle:"italic" }}>infinite content</span>
          </h2>
          <div style={{ display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(220px,1fr))",gap:28 }}>
            {[
              { n:"01",t:"Paste Your Content",d:"Drop in any blog post, idea, or transcript.",icon:FileText,grad:"linear-gradient(135deg,#7C3AED,#EC4899)" },
              { n:"02",t:"Choose Your Format",d:"Pick from 8 AI tools — social packs, emails, scripts and more.",icon:Layers,grad:"linear-gradient(135deg,#06B6D4,#6366F1)" },
              { n:"03",t:"Publish & Grow",d:"Copy and go live. Watch your output multiply across every channel.",icon:TrendingUp,grad:"linear-gradient(135deg,#10B981,#84CC16)" },
            ].map((step,i) => (
              <div key={i} style={{ textAlign:"center",padding:"28px 16px" }}>
                <div style={{ width:56,height:56,borderRadius:"50%",background:step.grad,display:"flex",alignItems:"center",justifyContent:"center",margin:"0 auto 18px",position:"relative",boxShadow:`0 8px 28px rgba(0,0,0,0.4)` }}>
                  <step.icon size={22} color="#fff"/>
                  <span style={{ position:"absolute",top:-8,right:-8,width:24,height:24,borderRadius:"50%",background:"#fff",color:"#080812",fontSize:9,fontWeight:900,display:"flex",alignItems:"center",justifyContent:"center",fontFamily:"'DM Mono',monospace" }}>{step.n}</span>
                </div>
                <h3 style={{ fontFamily:"'Syne',sans-serif",fontSize:19,fontWeight:800,marginBottom:10 }}>{step.t}</h3>
                <p style={{ color:"#9896B8",fontSize:14,lineHeight:1.7 }}>{step.d}</p>
              </div>
            ))}
          </div>
          <button onClick={() => setShowDemo(true)}
            style={{ marginTop:44,display:"inline-flex",alignItems:"center",gap:9,background:"rgba(124,58,237,0.12)",border:"1px solid rgba(124,58,237,0.4)",borderRadius:13,padding:"13px 26px",color:"#A78BFA",fontSize:14,fontWeight:700,cursor:"pointer",transition:"all .22s",fontFamily:"'Plus Jakarta Sans',sans-serif" }}
            onMouseEnter={e=>e.currentTarget.style.background="rgba(124,58,237,0.2)"}
            onMouseLeave={e=>e.currentTarget.style.background="rgba(124,58,237,0.12)"}>
            <Eye size={16}/>See It In Action — Interactive Demo
          </button>
        </div>
      </section>

      {/* ── PRICING ── */}
      <section style={{ padding:"100px 28px",maxWidth:1100,margin:"0 auto" }}>
        <div style={{ textAlign:"center",marginBottom:56 }}>
          <p style={{ fontSize:11,fontWeight:800,letterSpacing:"0.12em",textTransform:"uppercase",marginBottom:14,background:"linear-gradient(135deg,#FCD34D,#FB923C)",WebkitBackgroundClip:"text",WebkitTextFillColor:"transparent" }}>PRICING</p>
          <h2 style={{ fontFamily:"'Syne',sans-serif",fontSize:"clamp(28px,4vw,52px)",fontWeight:800,marginBottom:12 }}>Simple, transparent pricing</h2>
          <p style={{ color:"#9896B8" }}>Start free. Scale as you grow. Cancel any time.</p>
        </div>
        <div style={{ display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(280px,1fr))",gap:18,alignItems:"start" }}>
          {PLANS.map((p,i) => (
            <div key={i} className="pcard" style={{ background:p.grad,border:`1px solid ${p.border}`,boxShadow:p.feat?"0 0 0 1px rgba(124,58,237,0.3),0 24px 80px rgba(124,58,237,0.15)":undefined }}>
              {p.badge && (
                <div style={{ position:"absolute",top:-13,left:"50%",transform:"translateX(-50%)",background:"linear-gradient(135deg,#7C3AED,#EC4899)",borderRadius:22,padding:"5px 18px",fontSize:10,fontWeight:900,color:"#fff",letterSpacing:"0.08em",whiteSpace:"nowrap",boxShadow:"0 4px 14px rgba(124,58,237,0.5)" }}>{p.badge}</div>
              )}
              <p style={{ color:"#9896B8",fontSize:13,fontWeight:600,marginBottom:8 }}>{p.name}</p>
              <div style={{ display:"flex",alignItems:"baseline",gap:4,marginBottom:6 }}>
                <span style={{ fontFamily:"'Syne',sans-serif",fontSize:52,fontWeight:800,color:"#F2F0FF",lineHeight:1 }}>${p.price}</span>
                <span style={{ color:"#9896B8",fontSize:14 }}>/mo</span>
              </div>
              <p style={{ color:"#9896B8",fontSize:13,marginBottom:24 }}>{p.desc}</p>
              <button onClick={onEnter} className={p.feat?"grad-btn":""} style={{ width:"100%",padding:"13px 20px",borderRadius:11,border:p.feat?"none":`1px solid ${p.border}`,background:p.feat?undefined:"rgba(255,255,255,0.06)",color:"#F2F0FF",fontSize:13,fontWeight:800,cursor:"pointer",marginBottom:24,fontFamily:"'Plus Jakarta Sans',sans-serif",transition:"all .2s" }}>{p.cta}</button>
              <div style={{ display:"flex",flexDirection:"column",gap:11 }}>
                {p.features.map((f,fi) => (
                  <div key={fi} style={{ display:"flex",alignItems:"center",gap:10 }}>
                    <div style={{ width:18,height:18,borderRadius:"50%",background:"rgba(16,185,129,0.15)",border:"1px solid rgba(16,185,129,0.35)",display:"flex",alignItems:"center",justifyContent:"center",flexShrink:0 }}><Check size={10} color="#5EEAD4"/></div>
                    <span style={{ fontSize:13,color:"#9896B8" }}>{f}</span>
                  </div>
                ))}
              </div>
            </div>
          ))}
        </div>
      </section>

      {/* ── TESTIMONIALS ── */}
      <section style={{ padding:"80px 28px",background:"var(--bg2)",borderTop:"1px solid var(--border)",borderBottom:"1px solid var(--border)" }}>
        <div style={{ maxWidth:1100,margin:"0 auto" }}>
          <div style={{ textAlign:"center",marginBottom:48 }}>
            <p style={{ fontSize:11,fontWeight:800,letterSpacing:"0.12em",textTransform:"uppercase",marginBottom:14,background:"linear-gradient(135deg,#5EEAD4,#818CF8)",WebkitBackgroundClip:"text",WebkitTextFillColor:"transparent" }}>LOVED BY CREATORS</p>
            <h2 style={{ fontFamily:"'Syne',sans-serif",fontSize:"clamp(26px,4vw,46px)",fontWeight:800 }}>What our customers say</h2>
          </div>
          <div style={{ display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(290px,1fr))",gap:16 }}>
            {TESTIMONIALS.map((t,i) => (
              <div key={i} style={{ background:"rgba(255,255,255,0.03)",border:"1px solid var(--border-bright)",borderRadius:20,padding:26,transition:"all .25s" }}
                onMouseEnter={e=>{ e.currentTarget.style.borderColor="rgba(255,255,255,0.2)"; e.currentTarget.style.transform="translateY(-3px)"; }}
                onMouseLeave={e=>{ e.currentTarget.style.borderColor="var(--border-bright)"; e.currentTarget.style.transform=""; }}>
                <div style={{ display:"flex",gap:2,marginBottom:14 }}>{[...Array(t.stars)].map((_,si)=><Star key={si} size={13} fill="#F59E0B" color="#F59E0B"/>)}</div>
                <p style={{ fontSize:14,color:"#9896B8",lineHeight:1.75,marginBottom:22,fontStyle:"italic" }}>"{t.text}"</p>
                <div style={{ display:"flex",alignItems:"center",gap:11 }}>
                  <div style={{ width:38,height:38,borderRadius:"50%",background:t.grad,display:"flex",alignItems:"center",justifyContent:"center",fontSize:12,fontWeight:800,color:"#fff",boxShadow:`0 4px 14px rgba(0,0,0,0.3)` }}>{t.avatar}</div>
                  <div><p style={{ fontSize:13,fontWeight:700 }}>{t.name}</p><p style={{ fontSize:11,color:"#9896B8" }}>{t.role}</p></div>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* ── FINAL CTA ── */}
      <section style={{ padding:"110px 28px",textAlign:"center",position:"relative",overflow:"hidden" }}>
        <div className="orb" style={{ width:700,height:700,background:"radial-gradient(circle,rgba(124,58,237,0.1) 0%,rgba(236,72,153,0.06) 50%,transparent 70%)",top:"50%",left:"50%",transform:"translate(-50%,-50%)" }}/>
        <div style={{ position:"relative",zIndex:2,maxWidth:680,margin:"0 auto" }}>
          <h2 style={{ fontFamily:"'Syne',sans-serif",fontSize:"clamp(34px,5.5vw,66px)",fontWeight:800,marginBottom:22,lineHeight:1.08 }}>
            Your content strategy,<br/><span className="g-hero">on autopilot.</span>
          </h2>
          <p style={{ color:"#9896B8",fontSize:18,marginBottom:40,lineHeight:1.7 }}>Join 12,400+ businesses already multiplying their content output with AI.</p>
          <div style={{ display:"flex",gap:14,justifyContent:"center",flexWrap:"wrap" }}>
            <button onClick={onEnter} className="grad-btn" style={{ display:"inline-flex",alignItems:"center",gap:11,borderRadius:15,padding:"19px 42px",color:"#fff",fontSize:16,fontWeight:800,boxShadow:"0 12px 48px rgba(124,58,237,0.45)" }}>
              <Sparkles size={19}/>Start Generating Free
            </button>
            <button onClick={() => setShowDemo(true)}
              style={{ display:"inline-flex",alignItems:"center",gap:9,background:"rgba(255,255,255,0.06)",border:"1px solid var(--border-bright)",borderRadius:15,padding:"19px 30px",color:"#F2F0FF",fontSize:15,fontWeight:600,cursor:"pointer",transition:"all .22s",fontFamily:"'Plus Jakarta Sans',sans-serif" }}
              onMouseEnter={e=>e.currentTarget.style.background="rgba(255,255,255,0.1)"}
              onMouseLeave={e=>e.currentTarget.style.background="rgba(255,255,255,0.06)"}>
              <Eye size={16}/>Watch Demo
            </button>
          </div>
          <p style={{ marginTop:18,fontSize:13,color:"#5E5C80" }}>No credit card · Free forever tier · Cancel anytime</p>
        </div>
      </section>

      {/* ── FOOTER ── */}
      <footer style={{ background:"var(--bg2)",borderTop:"1px solid var(--border)",padding:"36px 28px" }}>
        <div style={{ maxWidth:1100,margin:"0 auto",display:"flex",alignItems:"center",justifyContent:"space-between",flexWrap:"wrap",gap:18 }}>
          <div style={{ display:"flex",alignItems:"center",gap:9 }}>
            <div style={{ width:30,height:30,borderRadius:8,background:"linear-gradient(135deg,#7C3AED,#EC4899)",display:"flex",alignItems:"center",justifyContent:"center" }}><Sparkles size={14} color="#fff"/></div>
            <span style={{ fontFamily:"'Syne',sans-serif",fontSize:15,fontWeight:800 }}>ContentForge AI</span>
          </div>
          <div style={{ display:"flex",gap:22,flexWrap:"wrap" }}>
            {["Privacy Policy","Terms","API Docs","Blog","Status","Contact"].map(l=>(
              <a key={l} href="#" style={{ fontSize:12,color:"#5E5C80",textDecoration:"none",transition:"color .18s" }}
                onMouseEnter={e=>e.target.style.color="#9896B8"} onMouseLeave={e=>e.target.style.color="#5E5C80"}>{l}</a>
            ))}
          </div>
          <p style={{ fontSize:12,color:"#5E5C80" }}>© 2026 ContentForge AI. All rights reserved.</p>
        </div>
      </footer>
    </div>
  );
}

/* ══════════════════════════════════════════════════════════════════
   ROOT
══════════════════════════════════════════════════════════════════ */
export default function App() {
  const [view, setView] = useState("landing");
  if (view === "app") return <AppDashboard onSignOut={() => setView("landing")}/>;
  return <LandingPage onEnter={() => setView("app")}/>;
}
